#include<stdio.h>

int main()
{
    float length, breadth;
    printf("Enter the value of the length of the rectange\n");
    scanf("%f",&length);

    printf("Enter the value of the breadth of the rectange\n");
    scanf("%f",&breadth);

    printf("Area of the Given Rectangle is %f",length*breadth);
    return 0;
}